<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.todayedu.ebag.teacher"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="14" />
	<uses-permission android:name="android.permission.INTERNET"/>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
	<uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/comm_app_name" >
        <activity
            android:name=".UIModule.WelcomeActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UIModule.FunctionActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- 一级Activity -->
         <activity
            android:name=".UIModule.ExamShowActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- 二级Activity -->
        <activity
            android:name=".UIModule.CFunctionActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.PCommentActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.ECorrectActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.SChooseActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.AExpandableActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- 三级Activity -->
        <activity
            android:name=".UIModule.SCStateActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.SCExamActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.ECSActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.AEPActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.PCCActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- 四级Activity -->
        <activity
            android:name=".UIModule.ECSSActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name=".UIModule.AEPSActivity"
            android:label="@string/comm_app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
    </application>

</manifest>